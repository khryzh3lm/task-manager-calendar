name: taskcalendar
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^3.0.0  # Ensure you have the latest version
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0   # latest

flutter:
  uses-material-design: true
